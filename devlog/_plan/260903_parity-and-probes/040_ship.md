# 040 - wp4: 옛 원장 갱신, 검증, 커밋, 푸시, 미러 (diff-level)

코드 변경 없음. wp2/wp3 결과를 하나의 배포 단위로 닫는다. 사용자가 이 goal 에 한해 push 를 사전 승인했다.

## 순서

1. 옛 원장에 갱신 노트. `devlog/_plan/260831_aside-official-parity/001_parity-ledger.md` 머리 문단 뒤에 삽입:

```markdown
> **Superseded (2026-09-03).** This ledger classifies the official `aside-browser` **v1**
> (165 lines) against the pre-1.26.902 `aside-jun`. Both sides have since been rewritten:
> the official skill is now v2 (103 lines) and `aside-jun` follows CLI 1.26.902. The
> current ledger is `devlog/_plan/260903_parity-and-probes/000_parity-ledger-v2.md`
> (PRESENT 47 / MISSING 20 / CONTRADICTS-CORRECTLY 8 / CONTRADICTS-WRONGLY 2). Keep this
> file as the v1 snapshot; do not act on its counts.
```

2. 검증. 020/030 의 "검증 (C)" 를 모두 실행하고 `devlog/_plan/260903_parity-and-probes/check-wp4.sh` 로 묶는다. 스크립트는 receipt 전에 커밋한다(지난 goal 의 CHECK-BINDING-01 교훈). 본문:

```bash
#!/usr/bin/env bash
# wp4 ship check: 020/030 C-checks + clean tree + on origin/main + mirror + PII
set -euo pipefail
cd "$(dirname "${BASH_SOURCE[0]}")/../../.."
fail=0
n=$(wc -l < aside-jun/SKILL.md); [ "$n" = "499" ] || { echo "SKILL.md $n lines, expected 499"; fail=1; }
rg -q 'daemon needs the GUI app' aside-jun/SKILL.md && { echo "v2-60 stale"; fail=1; }
rg -q '403' aside-jun/SKILL.md && { echo "403 claim"; fail=1; }
[ "$(rg -c 'remote-control.json' aside-jun/SKILL.md)" = "1" ] || { echo "remote-control.json"; fail=1; }
[ "$(rg -c 'the local GUI app need not be' aside-jun/SKILL.md)" = "1" ] || { echo "GUI wording"; fail=1; }
for pat in 'session archive' 'session delete' '/exit'; do
  rg -q "$pat" aside-jun/references/scheduling.md || { echo "missing: $pat"; fail=1; }
done
[ "$(rg -c 'never write into it' aside-jun/references/scheduling.md)" = "1" ] || { echo "memory fence"; fail=1; }
rg -U -q 'can write\s+durable facts into' aside-jun/references/scheduling.md && { echo "v2-45 regression"; fail=1; }
rg -q -- '--json' aside-jun/references/scheduling.md || { echo "memory --json"; fail=1; }
[ "$(rg -c 'checkPasswordVerificationRequired' aside-jun/references/credentials.md)" = "1" ] || { echo "touchid predicate"; fail=1; }
rg -q 'every unlock demands a live fingerprint' aside-jun/references/credentials.md && { echo "fingerprint claim"; fail=1; }
rg -q 'is unverified on 1.26.902' aside-jun/references/credentials.md && { echo "stale unverified"; fail=1; }
[ "$(rg -c '1.26.903.1631' aside-jun/references/credentials.md)" = "1" ] || { echo "daemon version cite"; fail=1; }
# the summary is generated: regenerate and require an identical result (date line aside)
tmp=$(mktemp); cp aside-jun/references/builtin-skills.md "$tmp"
bash aside-jun/scripts/refresh-builtin-summary.sh >/dev/null
diff <(rg -v '^Generated by' "$tmp") <(rg -v '^Generated by' aside-jun/references/builtin-skills.md) >/dev/null || { echo "builtin-skills drift"; fail=1; }
cp "$tmp" aside-jun/references/builtin-skills.md; rm -f "$tmp"
rg -q 'tell the user which skill' aside-jun/references/builtin-skills.md || { echo "skill notice"; fail=1; }
rg -q '^34 top-level skills and 16 site-specific' aside-jun/references/builtin-skills.md || { echo "counts"; fail=1; }
# the probes must have changed nothing on the live install
[ ! -f "$HOME/.aside/u/0/remote-control.json" ] || { echo "remote-control.json appeared"; fail=1; }
python3 -c "import json,sys;d=json.load(open('$HOME/.aside/u/0/passwords/settings.json'));sys.exit(0 if d['biometricUnlockEnabled'] is False else 1)" || { echo "biometricUnlockEnabled changed"; fail=1; }
[ "$(rg -c 'Superseded \(2026-09-03\)' devlog/_plan/260831_aside-official-parity/001_parity-ledger.md)" = "1" ] || { echo "old ledger note"; fail=1; }
if rg -n -i '\bsk-[a-z0-9]{10,}|bearer [a-z0-9]{20,}|"password"\s*:\s*"|[a-z0-9._-]+@[a-z0-9-]+\.(ac\.kr|com|net|org)\b' aside-jun README.md devlog/_plan/260903_parity-and-probes --glob '!check-wp4.sh'; then echo "PII"; fail=1; fi
[ -z "$(git status --short)" ] || { echo "dirty tree"; fail=1; }
git fetch -q origin && git merge-base --is-ancestor HEAD origin/main || { echo "not on origin/main"; fail=1; }
diff -rq aside-jun "$HOME/.codex/skills/aside-jun" || { echo "mirror drift"; fail=1; }
git diff --check || fail=1
[ $fail -eq 0 ] && echo "wp4 ship check: OK at $(git rev-parse --short HEAD)"
exit $fail
```
3. PII 스캔. `rg -n -i '\bsk-[a-z0-9]{10,}|bearer [a-z0-9]{20,}|"password"\s*:\s*"|[a-z0-9._-]+@[a-z0-9-]+\.(ac\.kr|com|net|org)\b' aside-jun README.md devlog/_plan/260903_parity-and-probes` 가 빈 결과여야 한다. 프로브 로그에 계정 루트 경로(`/Users/jun/...`)는 들어가지만 자격증명·이메일·핸들은 없어야 한다.
4. 커밋 (분리):
   - `[agent] docs: fold official v2 parity gaps and fix memory/remote-host claims` — aside-jun/ (scheduling.md, builtin-skills.md, scripts/, SKILL.md).
   - `[agent] docs: Remote Control and Touch ID wording follows the 260903 probes` — SKILL.md/credentials.md 중 wp3 몫.
   - `[agent] docs: parity unit 260903 with v2 ledger and probe evidence` — devlog/_plan/260903_parity-and-probes/ + 옛 원장 노트.
5. 푸시. `git push origin main`. 로그는 tracked 트리 밖 `.codexclaw/evidence/<session>/push.log` (gitignore).
6. 미러. `rsync -a --delete aside-jun/ ~/.codex/skills/aside-jun/` 후 `diff -rq` 빈 출력.
7. goalplan: c-1..c-4 에 evidence 기록, `cxc loop validate` 통과.

## 검증 (C)

- `git status --short` 빈 출력.
- `git merge-base --is-ancestor HEAD origin/main` 성공.
- `diff -rq aside-jun ~/.codex/skills/aside-jun` 빈 출력.
- `rg -c 'Superseded \(2026-09-03\)' devlog/_plan/260831_aside-official-parity/001_parity-ledger.md` == 1.
- 020/030 의 모든 C 항목 재실행 통과.
