# 030 - Ship

1. `quick_validate.py` with `/usr/local/bin/python3` against `aside-jun/`.
2. Line budget: SKILL.md under 500.
3. PII scan over tracked files.
4. Commit, push, confirm the remote ref line.
5. `rsync -a --delete` to both install dirs, `diff -r` both.
