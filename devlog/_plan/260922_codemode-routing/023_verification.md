# Verification record

## Independent fresh-reader result

Sol reader 01a0c81a-c209-7e90-a5e8-b0e1801e9c7f selected direct CLI for the
Codex/Claude shell-only scenario, --code-file/--cwd, bounded guest return and
explicit native Aside for unknown u2/remote context. It preserved no-install and
EDISABLED boundaries. It misread partial as boolean false; codemode.md now states
partial is an array and empty means partial.length === 0. Remote native routing
now repeats REPL mechanical versus exec judgment. Follow-up recheck requested.

## Worker integration

Codemode worker 01a0c813-de0e-76c0-b4ca-c3e2b71ae4d2 delivered one reference.
Main inspected it and retained original result envelopes, version contract and
issue44 limitation. Visualizer worker 01a0c813-ded2-71c1-bd59-960447840e7f
delivered one reference. Main corrected user-skill discovery from builtin CLI
lookup to the actual selected account file, removed truncated instruction reading
and ensured context variables were defined before use.

## Gate status

Skill frontmatter validation passed. The unit link gate currently fails because
compatibility.md is still being authored; this is not a completion claim. Final
gates will run after all workers stop writing.

Fresh-reader follow-up confirmed the corrected predicate and remote route; no
contradiction remains. This is model instruction-following evidence for the tested
scenarios, not enforcement across every agent or host.

## Fresh integrated gates

- `python3 devlog/_plan/260922_codemode-routing/022_verify.py --codemode-cli
  /tmp/aside-codemode-01a0c7ee-090/bin/codemode.mjs`: exit0, 15 Markdown files with
  no broken relative targets; exact documented local-batch returned both expected
  rows/excerpts and scope.coverage; browser discovery/argument schema passed
  without navigation; capped result, EROOT, EGUESTIMPORT, EDISABLED observed.
- Skill-creator quick_validate with existing cached PyYAML: Skill is valid, exit0.
- Mutation control in an isolated copy replaced the documented query TODO with
  NO_MATCH_CONTROL. The same checker exited1 at the expected fixture-row assertion.
  Repository documents were untouched; the temporary copy was removed.
- Raw MCP protocol proof remains in 001; no Codex/Claude attachment or live browser
  E2E was claimed. No runtime package, account, installed skill or sibling changed.

## Independent final semantic review

Fresh reviewer 01a0c81d-1d3c-7603-aab9-683001194fcc read the delivered entrypoint,
codemode, visualizer, exec and README. It checked links and 0.9.0 source/runtime
contracts: no P1/P2 semantic blockers, blocking_issues:[], VERDICT:PASS. Main
separately read the completed credentials/Windows/compatibility slice and checked
that current official guidance is distinguished from older measurements. All
workers have finished; final source-bound receipt follows in C.

## Outcome and limits

The repository now teaches direct caller CLI/MCP invocation and preserves native
Aside for dependent work and explicit nondefault contexts. SKILL.md is 106 lines
instead of 495. The no-auth local snippets and raw MCP protocol work against the
verified 0.9.0 snapshot. Windows commands and actual Codex/Claude MCP attachment
were not executed here; browser examples have schema proof, not browser E2E.
Only the explicitly authorized upstream issue #44 was published. No push, install,
account configuration change or sibling update occurred.

Final receipt command: `cxc receipt test --session <current-session> -- python3
 devlog/_plan/260922_codemode-routing/022_verify.py --codemode-cli <verified-0.9.0-source>/bin/codemode.mjs`.
Source-bound machine receipt is retained in this session's .codexclaw/evidence.
Final independent reviewer is closed; no background writer remains.
